## 代理模式
1. Subject：抽象角色，声明了真实对象和代理对象的共同接口；

    就是要做的事
2. Proxy：代理角色，实现了与真实对象相同的接口，所以在任何时刻都能够代理真实对象，并且代理对象内部包含了真实对象的引用，所以它可以操作真实对象，同时也可以附加其他的操作，相当于对真实对象进行封装。

    就是这个舍友
3. RealSubject：真实对象，是我们最终要引用的对象。---在这个
   
    我就是这个小妹妹 哈哈哈